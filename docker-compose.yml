# ==========================================
#  AI Background Remover — 一键部署
#  启动：docker compose up -d
#  访问：http://localhost:3000
# ==========================================

services:
  # ── 后端（FastAPI + rembg AI 推理） ──
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: bgremover-backend
    restart: unless-stopped
    # 不直接暴露端口到宿主机，仅通过 nginx 代理访问
    expose:
      - "8000"
    environment:
      - PYTHONUNBUFFERED=1
      - JWT_SECRET=${JWT_SECRET:-change_me_in_production_use_openssl_rand_hex_32}
    volumes:
      # 持久化 rembg 模型缓存
      - rembg-models:/root/.u2net
      # 持久化数据库数据
      - ./data:/app/data
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    # 内存限制（rembg 推理需 2-4GB，设为 4GB 安全值）
    deploy:
      resources:
        limits:
          memory: 4g
        reservations:
          memory: 1g

  # ── 前端（Nginx 静态服务 + API 代理） ──
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: bgremover-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:3000/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s

volumes:
  rembg-models:
    driver: local
